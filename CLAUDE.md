# CLAUDE.md

Read [AGENTS.md](AGENTS.md) — the maintainer guide for this repository — and
follow it. It holds the repository map, the CI gates, and the invariants
(fail-closed semantics, stable vocabularies, adapter-facts discipline) that
every change here is judged against. This file is deliberately a pointer so
the two cannot drift apart.
