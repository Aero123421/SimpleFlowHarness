# Project rules

Optional repository-specific rules.

- Name the canonical commands for test, lint, format, and package.
- Name architecture boundaries and existing helpers the agent should reuse.
- Name files that are generated rather than edited.
- Name compatibility or release obligations.
