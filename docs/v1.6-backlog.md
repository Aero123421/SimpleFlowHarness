# v1.6 residual backlog

2026-08-12の全体レビューで挙がった実装項目はv1.6.0で完了し、`CHANGELOG.md`へ
移しました。このファイルに残すのは、**検討したうえで変更しないと決めた境界**と、
release後も継続する評価課題だけです。

## 実作業のキューはGitHub issues

このファイルは作業キューではありません。着手可能な作業は
[GitHub issues](https://github.com/Aero123421/SimpleFlowHarness/issues)にあります。

この区別は実際に問題を起こしました。`AGENTS.md`がこのファイルを "the current
work queue" と指していた期間、唯一の未処理作業（実運用で踏んだ
`allow_empty` の教材不足、issue #25）はここに載っておらず、`AGENTS.md`に従う
agentは一度もそれを見ませんでした。以後、着手可能な作業はissueで追跡し、
このファイルは「やらないと決めたこと」の記録に限ります。

新しい作業を提案する前に、まずissuesを見て、次にこのファイルの境界を見てください。
境界に当たる提案は、境界そのものを議論してから出すべきものです。

## 継続する評価課題

### skills成果評価（P2）

`skills/evals/`は現在、発動判定（`trigger-cases.json`）を検査しますが、
「skillを読んだAIが書くflowは実際に改善するか」という成果評価は未整備です。
ponytail flowの一部を実projectで完走させ、run log、失敗分類、修正内容をpackへ
還流してから、再現可能な評価fixtureへ固定します。

### adapter factsの再検証

`src/preset.rs`の`LAST_VERIFIED`は、誰かが実際にlive CLIを再検証したときだけ
動かします。この日付が古いこと自体は誠実さの証拠であって欠陥ではありませんが、
CIが構造的に代われない唯一の鮮度時計でもあります。次のminorの前に7つのadapterを
実際に叩いて照合してください。日付だけを動かすのは禁止です。

## 検討して変更しない境界

- **stuckの処置は自動化しません。** stuckは、sfhが安全に決められない判断を
  人間へ返す機能です。改善範囲は通知と発見性までです。
- **step間prompt injectionをengineでは強制防御しません。**
  `{{steps.x.output}}`は生テキストで次のpromptへ入ります。context bundleの
  delimiter無害化と、skillsのuntrusted-evidence定石で緩和します。
- **run directoryを書き換えられる同一userへの完全防御はしません。**
  OS sandboxの境界です。writerのgit-worktree隔離とtree外`--runs-dir`を標準運用にします。
- **認証、rate limit、secret管理は外部toolと運用の責任です。** `doctor`、
  `tool_max_parallel`、flow外からのsecret注入を使います。first-class secret providerは
  具体的な需要が確認できてから検討します。
- **`--strict`は助言であってgateではありません**が、このrepositoryが同梱する
  flowは全数`--strict`を通します（意図的な例外1件は
  `examples/ponytail/VALIDATION.md`に記載）。読者に勧める検査を自分の同梱物に
  適用しないのは、助言ではなく怠慢です。
